Asemat Al Teeb — Static demo site

Files:
- index.html — Main page
- css/styles.css — Styles
- js/main.js — Simple interactivity

To view locally:
1. Open `index.html` in your browser (double-click or use "Open File").
2. Or serve with a static server (e.g., `npx serve` or `python -m http.server`).

Next steps:
- Add real product images and prices.
- Hook up a backend or e‑commerce/cart if you want to accept orders.
# Asemat-Al-Teeb
